<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="store.mapper.MemberMapper">
  
  <insert id="create" parameterType="Member">
  Insert into member_tb(mail, id, password, name, role, permission, attachment)
  Values(#{mail},#{id},#{password},#{name},#{role},#{permission},#{attachment})
  </insert>
  
  
  </mapper>